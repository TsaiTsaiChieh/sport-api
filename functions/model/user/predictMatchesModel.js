const modules = require('../../util/modules');

function predictMatches(args) {
  return new Promise(function(resolve, reject) {
    // 1. Check the match ids

    resolve(args);
  });
}

module.exports = predictMatches;
