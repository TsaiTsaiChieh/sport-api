service cloud.firestore {
  match /databases/{database}/documents {
    match /user/{userId} {
      allow read, write: if request.auth.uid == userId || resource.status==9;
      allow create:if request.auth.uid!=null;
      allow delete:if false;
    }
  }
}
